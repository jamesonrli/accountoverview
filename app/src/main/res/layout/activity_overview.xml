<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="OverviewActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:title="@string/app_name"
        app:title="@string/app_name"
        app:titleTextColor="@android:color/white"
        style="@style/AppToolbar" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/overview_fab"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/bullish"
        app:backgroundTint="@color/fab_green"
        style="@style/Fab" />

    <FrameLayout
        android:id="@+id/overview_full_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/main_toolbar"
        android:layout_above="@id/overview_fab" />

    <FrameLayout
        android:id="@+id/overview_sum_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/overview_full_container"
        android:layout_toLeftOf="@id/overview_fab" >

    </FrameLayout>

</RelativeLayout>
